#!/usr/bin/env python3
import math

"""Module for floor function with type annotations."""


def floor(n: float) -> int:
    """Return the floor of a float."""
    result = math.floor(n)
    return result
